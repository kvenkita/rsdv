% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostic_report.R
\name{diagnostic_report}
\alias{diagnostic_report}
\title{Generate a diagnostic (validity) report for synthetic data}
\usage{
diagnostic_report(real, synthetic, metadata)
}
\arguments{
\item{real}{A data frame of real data.}

\item{synthetic}{A data frame of synthetic data.}

\item{metadata}{An \code{rsdv_metadata} object.}
}
\value{
An \code{rsdv_diagnostic_report} object.
}
\description{
Checks whether synthetic data is \emph{structurally valid} against the real data
and metadata — independent of how closely it matches the real distributions
(that is the job of \code{\link[=quality_report]{quality_report()}}). Mirrors the SDMetrics
\code{DiagnosticReport} two-property hierarchy:
}
\details{
\itemize{
\item \strong{Data Validity} — per-column checks:
\itemize{
\item numerical: boundary adherence (fraction of values within the real
min/max range),
\item categorical: category adherence (fraction of values whose category was
seen in the real data),
\item boolean: always valid,
\item primary key: key uniqueness (all values unique and non-missing).
}
\item \strong{Data Structure} — fraction of expected columns present in the synthetic
data.
}

Missing (\code{NA}) values are excluded from adherence denominators, since
missingness is modeled separately.
}
\examples{
\donttest{
meta  <- metadata(adult_income)
syn   <- gaussian_copula_synthesizer(meta) |> fit(adult_income)
synth <- sample(syn, n = 500)
diagnostic_report(adult_income, synth, meta)
}
}
