% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaussian_copula.R
\name{gaussian_copula_synthesizer}
\alias{gaussian_copula_synthesizer}
\title{Create a Gaussian Copula synthesizer}
\usage{
gaussian_copula_synthesizer(
  metadata,
  enforce_min_max = TRUE,
  numerical_distributions = list(),
  default_distribution = "auto"
)
}
\arguments{
\item{metadata}{An \code{rsdv_metadata} object.}

\item{enforce_min_max}{Logical. Clamp sampled numerical values to the
observed range. Default \code{TRUE}.}

\item{numerical_distributions}{Optional named character vector/list mapping
numerical column names to a distribution in \code{"norm"}, \code{"beta"}, \code{"gamma"},
\code{"truncnorm"}, \code{"uniform"}, or \code{"auto"}.}

\item{default_distribution}{Distribution used for numerical columns not named
in \code{numerical_distributions}. \code{"auto"} (default) selects the best-fitting
family per column by Kolmogorov-Smirnov distance.}
}
\value{
An unfitted \code{gaussian_copula_synthesizer} object.
}
\description{
Fits a single Gaussian copula over \strong{all} modeled columns. Numerical
columns use a fitted parametric marginal (see \code{default_distribution});
categorical and boolean columns are embedded into the copula via their
cumulative-frequency intervals, so cross-column dependence (numeric vs.
categorical, categorical vs. categorical) is preserved.
}
\examples{
\donttest{
meta <- metadata(adult_income) |>
  set_column_type("age", "numerical") |>
  set_column_type("occupation", "categorical")
syn <- gaussian_copula_synthesizer(meta, default_distribution = "auto")
syn <- fit(syn, adult_income)
}
}
