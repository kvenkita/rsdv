% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/privacy_metrics.R
\name{attribute_disclosure_risk}
\alias{attribute_disclosure_risk}
\title{Attribute disclosure risk}
\usage{
attribute_disclosure_risk(real, synthetic, sensitive_col, known_cols, k = 1L)
}
\arguments{
\item{real, synthetic}{Data frames.}

\item{sensitive_col}{Name of the column to protect.}

\item{known_cols}{Character vector of columns assumed known to an adversary.}

\item{k}{Number of nearest neighbors used in inference.}
}
\value{
A scalar in [0, 1]; lower = more private.
}
\description{
Estimates the fraction of synthetic rows where a sensitive column value
can be correctly inferred from known columns via a k-NN lookup in the
real training data.
}
\examples{
real <- data.frame(age = sample(20:60, 50, replace = TRUE),
                   income = sample(c("low", "high"), 50, replace = TRUE),
                   stringsAsFactors = FALSE)
syn  <- real[sample(50), ]
attribute_disclosure_risk(real, syn, sensitive_col = "income", known_cols = "age")
}
