% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality_metrics.R
\name{contingency_similarity}
\alias{contingency_similarity}
\title{Contingency similarity between real and synthetic categorical column pairs}
\usage{
contingency_similarity(real, synthetic, meta)
}
\arguments{
\item{real}{A data frame of real data.}

\item{synthetic}{A data frame of synthetic data.}

\item{meta}{An \code{rsdv_metadata} object.}
}
\value{
A list with \code{pairs} (a tibble of \code{column_1}, \code{column_2}, \code{score}) and
\code{score} (the mean over pairs). \code{score} is \code{NA_real_} when there are fewer
than two categorical columns — there is no dependence to measure, so
propagating \code{NA} (rather than \code{1}) avoids overstating fidelity in the
aggregated quality report.
}
\description{
For each pair of categorical columns, compares the joint (normalized
contingency) distributions of real and synthetic data via total variation
distance, scoring \code{1 - TVD} (the SDMetrics \code{ContingencySimilarity} score).
This is the categorical analogue of correlation similarity and captures
categorical-vs-categorical dependence.
}
\examples{
\donttest{
meta  <- metadata(adult_income)
syn   <- gaussian_copula_synthesizer(meta) |> fit(adult_income)
synth <- sample(syn, n = 500)
contingency_similarity(adult_income, synth, meta)
}
}
