% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthesizer.R
\name{sample}
\alias{sample}
\title{Sample synthetic rows from a fitted synthesizer}
\usage{
sample(x, n = NULL, ...)
}
\arguments{
\item{x}{A fitted synthesizer object, or a vector for \code{\link[base:sample]{base::sample()}} compat.}

\item{n}{Number of synthetic rows to generate (synthesizer path), or
sample size (base::sample path).}

\item{...}{Additional arguments passed to the method or to \code{\link[base:sample]{base::sample()}}.}
}
\value{
When \code{x} inherits from \code{rsdv_synthesizer}, a data frame of \code{n}
synthetic rows whose columns match the metadata. When \code{x} is any other
object, the value returned by \code{\link[base:sample]{base::sample()}} — typically a vector of
the same type as \code{x} and length \code{n}.
}
\description{
Dispatches to the synthesizer-specific method when \code{x} is an
\code{rsdv_synthesizer}. For plain R vectors, integers, or characters it
falls back to \code{\link[base:sample]{base::sample()}}, preserving backward compatibility.
}
\examples{
# Falls back to base::sample for non-synthesizer objects:
sample(1:10, 3)

\donttest{
meta  <- metadata(adult_income) |>
  set_column_type("age",    "numerical") |>
  set_column_type("income", "categorical")
syn   <- gaussian_copula_synthesizer(meta) |> fit(adult_income)
synth <- sample(syn, n = 100)
head(synth)
}
}
